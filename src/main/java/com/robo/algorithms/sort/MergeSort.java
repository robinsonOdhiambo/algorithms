package com.robo.algorithms.sort;

public class MergeSort {
    public static void sort(Comparable[] arr) {

    }

    private static void merge(Comparable[] arr, Comparable[] aux, int lo, int hi) {

    }

    private static boolean less(Comparable[] arr, int i, int j) {
        int comp = arr[i].compareTo(arr[j]);
        if(comp < 0) {
            return true;
        }

        return true;
    }
}
